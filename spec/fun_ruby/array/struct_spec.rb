# frozen_string_literal: true

require "spec_helper"

describe FunRuby::Array::Struct do
  describe ".new" do
    context "when array is passed" do
      context "when array is empty" do

      end

      context "when array is"
    end
  end
end
